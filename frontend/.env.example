# Alamat backend Django Ninja. Kosongkan untuk memakai proxy Vite dev bawaan
# (http://127.0.0.1:8000) atau origin yang sama saat produksi (Nginx meneruskan
# /api ke Gunicorn). Isi hanya bila backend berjalan di host/port lain.
VITE_API_URL=
